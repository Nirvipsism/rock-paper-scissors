console.log("Hello, World!")

function getComputerChoice {
    let randomNum = Math.floor(Math.random() * 3);
    if (randomNum === O) {
        return "Rock";
    } else if (randomNum === 1) {
        return "Paper";
    } else { 
        return "Scissors";
    }
}